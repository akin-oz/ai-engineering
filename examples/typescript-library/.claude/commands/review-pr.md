Review the current pull request against this repository's rules.

Work through compatibility first, then tests, then documentation. For each
finding, name the specific input or caller that breaks, and point at the file
and line. Separate what must change before merge from what is worth doing later,
and say explicitly which parts of the diff you checked and found sound.
